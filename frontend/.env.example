# Leave empty when the frontend and Vercel API function are deployed together.
# For a separately deployed Express backend, set the full URL, e.g. https://edurisk-api.example.com/api
VITE_API_URL=/api
