import { Icon } from '../Icon';
export function StatCard({ label, value, detail, tone = 'indigo', trend, icon }: { label: string; value: string | number; detail: string; tone?: string; trend?: string; icon: string }) { return <div className={`stat-card ${tone}`}><div className="stat-card-top"><span className="stat-icon"><Icon name={icon} size={19}/></span>{trend && <span className={`trend ${trend.startsWith('-') ? 'negative' : ''}`}><Icon name={trend.startsWith('-') ? 'down' : 'up'} size={13}/>{trend}</span>}</div><div className="stat-value">{value}</div><div className="stat-label">{label}</div><div className="stat-detail">{detail}</div></div>; }
