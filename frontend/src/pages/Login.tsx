import { FormEvent, useEffect, useState } from 'react';
import { Navigate, useNavigate } from 'react-router-dom';
import { Icon } from '../components/Icon';
import { useAuth } from '../context/AuthContext';
export default function Login() {
  const { user, login } = useAuth(); const navigate = useNavigate(); const [email, setEmail] = useState('admin@edurisk.ai'); const [password, setPassword] = useState('password'); const [role, setRole] = useState('admin'); const [error, setError] = useState(''); const [working, setWorking] = useState(false);
  useEffect(() => { if (user) navigate('/'); }, [user, navigate]);
  if (user) return <Navigate to="/" replace/>;
  async function submit(event: FormEvent) { event.preventDefault(); setError(''); setWorking(true); try { await login(email, password, role); navigate('/'); } catch (e) { setError(e instanceof Error ? e.message : 'Unable to sign in'); } finally { setWorking(false); } }
  return <div className="login-page"><div className="login-visual"><div className="login-visual-content"><div className="brand light"><span className="brand-mark">E</span><div><strong>EduRisk<span> AI</span></strong><small>Academic intelligence</small></div></div><div className="visual-copy"><p className="kicker">EARLY SIGNALS. BETTER OUTCOMES.</p><h1>See the student<br/><em>behind the score.</em></h1><p>One calm, clear workspace to turn academic signals into timely support.</p></div><div className="visual-insight"><div className="insight-icon"><Icon name="sparkles" size={18}/></div><div><strong>Students thrive when support starts early.</strong><span>Data-informed decisions for the people who make the difference.</span></div></div></div><div className="login-orbit orbit-one"/><div className="login-orbit orbit-two"/><div className="login-dots"/></div><div className="login-panel"><div className="mobile-brand"><div className="brand"><span className="brand-mark">E</span><div><strong>EduRisk<span> AI</span></strong><small>Academic intelligence</small></div></div></div><div className="login-form-wrap"><div className="login-heading"><p className="kicker">WELCOME BACK</p><h2>Sign in to your workspace</h2><p>Use your institution account to continue.</p></div><form onSubmit={submit}><label>Email address<div className="input-wrap"><Icon name="user" size={17}/><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@institution.edu" required/></div></label><label>Password<div className="input-wrap"><Icon name="shield" size={17}/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Enter your password" required/></div></label><div className="form-row"><label className="role-label">Sign in as<select value={role} onChange={e=>setRole(e.target.value)}><option value="admin">Administrator</option><option value="faculty">Faculty member</option><option value="mentor">Mentor</option></select><Icon name="chevronDown" size={15}/></label><a href="#forgot" className="forgot-link">Forgot password?</a></div>{error && <div className="form-error"><Icon name="alert" size={15}/>{error}</div>}<button className="button primary full-width" disabled={working}>{working ? <><span className="button-spinner"/>Signing in…</> : <>Sign in <Icon name="arrowUp" size={16}/></>}</button></form><div className="demo-note"><Icon name="sparkles" size={14}/><span><strong>Demo workspace</strong> Use admin@edurisk.ai with password: password.</span></div><p className="login-legal">By continuing, you agree to EduRisk AI’s <a href="#terms">Terms of Service</a> and <a href="#privacy">Privacy Policy</a>.</p></div><div className="login-footer">© 2026 EduRisk AI <span>•</span> Built for student success</div></div></div>;
}
